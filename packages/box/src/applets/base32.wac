// Encoded, with a trailing newline.

import { Cli, Core } from "../../../platform/src/platform.wac";
import { Args } from "../lib/args.wac";
import { Input, readInput } from "../lib/input.wac";
import { withNewline } from "../lib/bytes.wac";
import { encode as b32, ALPHABET_STANDARD } from "../../../codec/src/base32.wac";

export i32 base32(Core core, Cli cli, Args a) {
  Input in = readInput(core, cli, a, 0);
  if (!in.ok) { return 1; }
  cli.write(withNewline(b32(in.bytes, ALPHABET_STANDARD(), true)));
  return 0;
}
